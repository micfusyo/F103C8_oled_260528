{
    "configurations": [
        {
            "name": "STM32",
            "compileCommands": "${workspaceFolder}/build/Debug/compile_commands.json",
            "includePath": "${workspaceFolder}/../shared_libs/Inc"
        }
    ]
}